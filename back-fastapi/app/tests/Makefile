# Path: app/tests/Makefile

default:
	pytest --disable-warnings

test:
	pytest -v --disable-warnings

test-auth:
	pytest -v --disable-warnings test_auth.py

test-accounts:
	pytest -v --disable-warnings test_accounts_api.py
